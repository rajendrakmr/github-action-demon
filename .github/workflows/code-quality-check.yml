name: Code Quality Check

on:
    workflow_call:


jobs:
    code-quality-check:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout Code
              uses: actions/checkout@v4
              
